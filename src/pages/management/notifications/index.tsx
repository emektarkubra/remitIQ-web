import withLayout from "../../../layout/withLayout"

const Notifications = () => {
    return (
        <div>
            Notifications page
        </div>
    )
}

export default withLayout(<Notifications />)